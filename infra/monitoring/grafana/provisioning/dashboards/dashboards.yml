apiVersion: 1

providers:
  - name: "Local Dashboards"
    orgId: 1
    folder: "Local Infrastructure"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
